Use 
XDP - XML Diff and Patch.exe or
"XDP - XML Diff and Patch [- Debug].bat" to launch the Application:
("cmd /C start javaw -jar cmd /C start javaw -jar "XDP - XML Diff and Patch.jar" /B ")

xmlFiles folder contains some XML Files

costs.cfg contains the pre-defiened costs of the userand can be changed in the settings tab.

Output Files will be in the output folder in the directory of the Application.

[ ] : Optional